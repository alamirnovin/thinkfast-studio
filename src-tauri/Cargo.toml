[package]
name = "thinking-fast-studio"
version = "0.1.0"
description = "A friendly desktop studio for fast, reviewable text decisions"
authors = ["You"]
edition = "2021"

[lib]
name = "thinking_fast_studio_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-shell = "2"
